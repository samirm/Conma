

use std::collections::HashMap;

let mut contacts = HashMap::new();

contacts.insert(String::from("Farid M"), 10);
contacts.insert(String::from("Richard L"), 50);

fn main() {
    println!("Hello, world!");
}
